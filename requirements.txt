# Core Python Libraries
streamlit==1.33.0
Pillow==10.2.0
python-dotenv==1.0.1

# OpenAI Client Library
openai==1.14.0

# Base Python Libraries (built-in, no need to specify)
# requests library for potential HTTP requests
requests==2.31.0
